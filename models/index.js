const Categoria = required('./categoria');
const Role = required('./role');
const Usuario = required('./usuario');
const Server = required('./server');
const Producto = required('./producto');

//Ordenar alfabeticamente
export {
    Categoria,
    Role,
    Usuario,
    Server,
    Producto
}